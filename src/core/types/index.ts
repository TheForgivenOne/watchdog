export interface Subdog {
  id: string;
  name: string;
  description: string;
  path: string;
  icon: string;
  color: string;
}

export interface NavItem {
  label: string;
  path: string;
  icon: string;
}
